# 上下文组模式分行功能改进

## 功能概述

改进了上下文组模式（Context Group Mode）的分行功能，添加了可视化的译文预览区域，用户可以更直观地插入和管理分行标记。

## 改进内容

### 1. 可视化译文预览

- 在译文输入框（textarea）下方添加了实时同步的预览区域
- 预览区域不可直接编辑，但会实时显示译文内容按 `---SPLIT---` 标记分割的效果
- 每个分段显示为独立的行

### 2. 可视化分行操作

用户可以通过点击预览区域中的分隔线来管理分行：

- **插入分行标记**：点击普通分隔线（`+` 图标），在该位置插入 `---SPLIT---`
- **删除分行标记**：点击激活的分隔线（`×` 图标），移除该位置的 `---SPLIT---`
- 分隔线在鼠标悬停时会高亮显示

### 3. 增强的拆分逻辑

`splitGroupTranslation()` 函数现在支持三种拆分模式（按优先级）：

1. **按 `---SPLIT---` 标记拆分**（优先级最高）
   - 精确按用户插入的标记位置拆分
   
2. **按换行符拆分**（优先级中等）
   - 如果没有 `---SPLIT---` 标记但有换行符，则按换行拆分
   
3. **按百分比位置拆分**（优先级最低）
   - 使用原有的百分比算法拆分

## 用户界面变化

### 新增元素

1. **预览容器** (`.context-group-preview-container`)
   - 深色背景，带圆角和边框
   - 包含标签和预览行区域

2. **预览标签** (`.context-group-preview-label`)
   - 显示提示文本："译文预览（点击行间分隔线插入/移除分行标记）"
   - 小号字体，灰色文本

3. **预览行** (`.preview-line-row`)
   - 显示每个分段的内容
   - 空行显示为 "（空行）" 并半透明

4. **分隔线** (`.preview-line-separator`)
   - 可点击的交互元素
   - 未激活状态：显示 `+` 图标（插入）
   - 激活状态：显示 `×` 图标（删除）
   - 鼠标悬停时高亮并放大

## 技术实现

### 修改的文件

1. **renderer/app/entries.js**
   - 第 290-319 行：增强 `splitGroupTranslation()` 函数
   - 第 362-452 行：添加预览容器和交互逻辑
   - 第 485-487 行：AI翻译完成后刷新预览
   - 第 503-506 行：调整DOM结构，添加预览容器

2. **renderer/renderer.js**
   - 第 198 行：添加国际化文本 `context.groupPreviewLabel`

3. **renderer/styles.css**
   - 第 476-565 行：添加预览组件的CSS样式

### 关键函数

```javascript
function renderPreviewLines() {
  // 1. 清空预览区域
  // 2. 按 ---SPLIT--- 拆分文本
  // 3. 渲染每个分段为一行
  // 4. 在行之间添加可点击的分隔线
  // 5. 为不足的行数添加空行占位
}
```

## 使用方法

1. 在上下文组模式下选择多个条目
2. 在译文输入框中输入整体译文
3. 在下方预览区域查看分段效果
4. 点击行间的分隔线插入或删除 `---SPLIT---` 标记
5. 点击"应用到所选"按钮，译文会按标记拆分并填充到各个条目

## 优势

- **更直观**：可视化显示分段效果，无需猜测
- **更便捷**：点击操作代替手动输入标记
- **实时反馈**：输入即时同步到预览
- **容错性强**：支持多种拆分模式，兼容旧的使用习惯

## 兼容性

- 完全向后兼容，旧的 `---SPLIT---` 手动输入方式仍然有效
- 不影响单条模式的使用
- 保留了原有的提示文本和按换行拆分的功能
